/**
 * This class is autogenerated by DtoProcessor using Dto annotation.
 */
package ${packageName};

#foreach($import in $imports)
import ${import};
#end

public class ${targetClass}To${sourceClass}Mapper {

    public static ${targetClass} toDto(${sourceClass} source) {
        ${targetClass} ret = new ${targetClass}();

        #foreach($field in $fields)
            ret.set${field}(source.get${field}());
        #end

        return ret;
    }
}
